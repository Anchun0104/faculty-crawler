# 高校教师数据采集项目任务转接说明

## 1. 转接快照

- 转接日期：2026-07-22（Asia/Shanghai）。
- 当前分支：`master`。
- 当前提交：`d80f108`（`fix: wait for delayed directory rendering`）。
- ZIP 中的源码、测试和 `output` 是打包时工作目录的真实快照，应作为本次转接的内容基准。
- `repository.bundle` 只保存已经提交的 Git 历史，不包含未提交或未跟踪的 Excel；这些文件以 ZIP 中的工作目录快照为准。
- `.venv`、`.git`、`__pycache__`、`.pyc`、旧 `dist` 压缩包均被排除。

## 2. 项目目标

项目从公开的高校院系教师目录采集姓名、学术职称、个人主页 URL 和目录页公开邮箱，经过过滤、规范化和去重后导出 Excel。解析策略面向多校站点的通用结构，不应通过针对单个 URL 的硬编码补丁解决兼容问题。

## 3. 代码结构

- `main.py`：单网址命令行入口，负责参数检查、调用爬虫并导出正式结果和待确认职称结果。
- `desktop_app.py`：Windows 批量桌面入口；顺序运行多个网址、显示状态，并保存经过脱敏的诊断日志。
- `crawler/faculty_crawler.py`：Playwright 加载、动态结果交互、分页编排、诊断和 Excel 导出。
- `crawler/parsers.py`：纯 HTML 解析、候选识别、字段提取、过滤、URL 规范化和去重，是兼容性优化的主要位置。
- `crawler/batch.py`：批量任务准备、文件命名、失败隔离和日志脱敏。
- `tests/`：解析器、爬虫诊断、CLI、GUI/日志、导出及打包回归测试。
- `CODEX_PARSER_RULES.md`：修改解析逻辑前必须阅读的规则，包括向后兼容、禁止站点特判、测试矩阵和完成标准。
- `output/`：现有正式采集结果；`output/pending_title/` 保存有可靠人物证据但职称仍需确认的记录。

## 4. 环境与首次安装

要求 Windows、Python 3.11+ 和网络连接。最简单的方式是在项目根目录双击一次 `setup.bat`；它会创建 `.venv`、安装 `requirements.txt`，并安装 Playwright Chromium。

手动安装方式：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m playwright install chromium
```

不要复制其他电脑生成的 `.venv`。虚拟环境可能记录原电脑的解释器绝对路径，应在每台电脑上重新创建。

## 5. 运行方式

桌面批量运行：双击 `start.bat`，每行输入一个教师目录网址，选择输出目录后顺序执行。

命令行单网址调试：

```powershell
.\.venv\Scripts\python.exe main.py <URL> --output output\result.xlsx --timeout 30000 --verbose
```

重新生成普通用户发布包：

```powershell
.\.venv\Scripts\python.exe build_release.py
```

重新生成本开发转接包：

```powershell
.\.venv\Scripts\python.exe build_handoff.py
```

## 6. 测试与修改纪律

修改解析或爬取行为前先阅读 `CODEX_PARSER_RULES.md`，为目标页面结构写一个最小失败测试，确认测试因缺少该能力而失败，再做最小通用修复。每次修改后运行：

```powershell
python -m unittest discover -s tests -v
```

必须同时验证新站点用例与所有既有用例。不要顺手重构无关代码；站点特有字符串、域名白名单或 URL 特判通常不是可接受的长期方案。

## 7. 输出与日志

- 正式 Excel 列为 `Name`、`Title`、`Profile_URL`、`Email`。
- 职称证据不足但人物证据可靠的记录写入 `output/pending_title`，不混入正式结果。
- 桌面模式日志位于所选输出目录的 `logs/run_*.log`。
- 分享日志前仍应快速检查；程序会脱敏 Cookie、Authorization、签名查询参数和本机路径，但不要主动把登录态或私有页面交给爬虫。
- 重复运行时输出文件名会自动避让，避免无提示覆盖旧 Excel。

## 8. 当前能力

- 支持 Playwright Chromium 和 JavaScript 渲染页面。
- 支持普通下一页链接、查询参数分页、动态结果区域、页面大小选择和延迟渲染等待。
- 支持卡片、表格、链接列表、手风琴、分组角色、嵌入 JSON 和多种非标准教师目录布局。
- 支持学术/行政/荣休等区域过滤、个人主页 URL 规范化、身份查询参数保留及跟踪参数移除。
- 支持目录页邮箱提取、记录去重、低覆盖诊断以及职称待确认的独立导出。
- 支持桌面批量运行；单个网址失败后继续处理后续任务。

## 9. 已知限制

- 解析是启发式和尽力而为；网站改版后可能出现漏采或误采，必须依靠日志与回归测试逐步扩展。
- 当前不递归进入每个个人主页补全邮箱，也没有 AI 解析阶段。
- 当前没有数据库、并发任务、断点恢复、代理池或任务调度服务。
- 不以绕过登录、验证码、访问控制或网站封禁为目标。新增访问适配必须遵守网站条款、robots 策略、合理限速和数据使用要求。
- 某些页面在无头浏览器、地区网络或 Cookie 状态下表现不同；先保存可复现日志，再修改代码。

## 10. 后续优化建议

1. 每次选择一个失败或低覆盖站点，保留最小 HTML 结构作为回归测试，再进行通用解析改进。
2. 将访问层逐步补充为合规的限速、重试、指数退避、会话保持和站点响应诊断；不要加入验证码或访问控制绕过。
3. 把“进入个人主页补全公开学校邮箱”设计为独立第二阶段：读取第一阶段 Excel，只处理有效主页 URL，并保留来源、提取方式和失败原因。
4. 如引入 AI，只作为规则解析失败后的可选兜底；必须限制成本、禁止猜测邮箱，并保存来源证据和置信信息。
5. 后续若任务量扩大，再考虑增量任务状态、断点恢复和结构化数据库，不要提前重构现有稳定解析流程。

## 11. Git 历史

检查 bundle：

```powershell
git bundle verify repository.bundle
```

从 bundle 克隆一份只含已提交历史的仓库：

```powershell
git clone repository.bundle faculty-crawler-history
```

注意：克隆结果不包含 ZIP 快照中的未提交 Excel。需要继续当前任务时，应以解压后的源码和 `output` 为准；`repository.bundle` 主要用于查看历史提交、比较设计演进或恢复已提交版本。
